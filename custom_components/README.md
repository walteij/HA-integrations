# Custom Components

This folder contains the actual Home Assistant custom integrations that HACS can install.

Each integration must live in its own subfolder named after its domain:

- `custom_components/mlb_standings`

If you add another integration later, create a new folder here and add a matching documentation folder under `integrations/`.